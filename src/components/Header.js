let Header = () => {
    return (
      <header>
        <h1>Media Eval</h1>
        {/* <p>Items will persist in the browser local storage</p> */}
      </header>
    );
  };
  export default Header;